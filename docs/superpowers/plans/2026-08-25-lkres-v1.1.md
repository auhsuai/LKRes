# LKRes v1.1 Implementation Plan (cải tiến theo feedback user)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Nâng cấp LKRes sau feedback user: (1) tab Dải màu chế độ Auto lai A+ — mặc định 3 dải, +/− thêm/bớt, chip chia hàng theo LOẠI giá trị có nhãn cố định, chạm dải để sửa, Undo + Xoá tất cả; (2) Search giá trị → nhiều tổ hợp màu song song kèm chip dung sai/TCR; (3) Tab Cài đặt mới + lưu state bằng DataStore + lịch sử tra gần nhất; (4) Tham khảo chuyển bảng cột cố định; (5) EIA-96 tối ưu lag (research best-practice trước).

**Architecture:** Giữ nguyên kiến trúc v1.0 (core/ thuần Kotlin là nguồn sự thật, UI chỉ render). Thêm: `core/ValueParser.kt`, `core/ValueToColors.kt`, `core/BandRole.kt`; `data/LkResStore.kt` (DataStore — lớp Android duy nhất); tab Cài đặt thứ 4. Build/test qua GitHub Actions CI như v1.0 (local không có JDK).

**Tech Stack:** Kotlin, Compose Material 3, Navigation Compose, DataStore Preferences, JUnit4.

**Spec gốc:** `docs/superpowers/specs/2026-08-25-lkres-design.md` + plan v1.0 `docs/superpowers/plans/2026-08-25-lkres.md` (đã hoàn thành 10 task).

## Global Constraints (kế thừa v1.0 + bổ sung)

- Build/test CHỈ qua GitHub Actions (workflow `.github/workflows/android.yml` có sẵn). Poll `GET https://api.github.com/repos/auhsuai/LKRes/actions/runs?per_page=1` (header `User-Agent` bất kỳ, 30s/lần, tối đa 15 phút). RED→GREEN qua CI cho mọi core task: commit test trước → CI failure → code → CI success. Ghi run id vào report.
- TRƯỚC KHI code: DuckDuckGo MCP + context7/webfetch developer.android.com tra API đúng version trong `app/build.gradle.kts` (BOM 2026.08.00). context7 hết quota → fallback webfetch, ghi nguồn trong report. CẤM đoán API, CẤM bịa nguồn; không dùng API mới thì ghi `N/A`.
- `core/` thuần Kotlin KHÔNG import Android/Compose. Chỉ `data/LkResStore.kt` được đụng Android (DataStore + Context).
- KHÔNG hard-code số bảng trong UI — mọi số từ `core/`. Ngoại lệ: text nhãn/tiêu đề/hint.
- Encoding UTF-8 sạch cho tiếng Việt — công cụ ghi file chuẩn, không qua PowerShell Set-Content.
- KHÔNG đụng `.github/workflows/`, không thêm permission vào Manifest (DataStore không cần permission).
- Platform declaration clash: property `private set` + hàm trùng tên JVM = lỗi (bài học v1.0 Task 7) — dùng pattern `_backingField` + getter khi cần hàm public trùng tên.
- Dispatch TUẦN TỰ, một subagent mỗi task, review sau mỗi task (5C.4).
- Task ≥ 4 file → tự chia checkpoint trong report (5B.8.5).

---

### Task 11: Core — BandRole + ValueParser (TDD)

**Files:**
- Create: `app/src/main/java/com/lkres/app/core/BandRole.kt`
- Create: `app/src/main/java/com/lkres/app/core/ValueParser.kt`
- Test: `app/src/test/java/com/lkres/app/core/ValueParserTest.kt`

**Interfaces:**
- Produces:
  - `enum class BandRole { DIGIT, MULTIPLIER, TOLERANCE, TCR }` + `fun rolesFor(bandCount: Int): List<BandRole>` (3 dải: [DIGIT,DIGIT,MULTIPLIER]; 4: +TOLERANCE; 5: [DIGIT×3,MULTIPLIER,TOLERANCE]; 6: +TCR; ngoài 3..6 → emptyList)
  - `sealed interface ValueParseResult { data class Success(val ohms: Double); data class Error(val kind: ValueErrorKind) }`
  - `enum class ValueErrorKind(val message: String)`: EMPTY, INVALID_FORMAT ("VD hợp lệ: 4700 · 4,7k · 4.7k · 4k7 · 1M"), NON_POSITIVE ("Giá trị phải lớn hơn 0"), SMD_STYLE ("Mã dạng 4R7 là mã SMD — hãy tra ở tab SMD")
  - `object ValueParser { fun parse(raw: String): ValueParseResult }`

- [ ] **Step 1: Viết test FAIL trước** — `ValueParserTest.kt`:

```kotlin
package com.lkres.app.core

import org.junit.Assert.assertEquals
import org.junit.Assert.assertTrue
import org.junit.Test

class ValueParserTest {

    private fun ohms(raw: String): Double {
        val r = ValueParser.parse(raw)
        assertTrue("'$raw' phải Success, thực tế $r", r is ValueParseResult.Success)
        return (r as ValueParseResult.Success).ohms
    }

    private fun err(raw: String): ValueErrorKind {
        val r = ValueParser.parse(raw)
        assertTrue("'$raw' phải Error, thực tế $r", r is ValueParseResult.Error)
        return (r as ValueParseResult.Error).kind
    }

    @Test fun `số trần 4700`() = assertEquals(4700.0, ohms("4700"), 1e-9)
    @Test fun `có đơn vị 1k`() = assertEquals(1000.0, ohms("1k"), 1e-9)
    @Test fun `hoa thường 1K và 1k bằng nhau`() = assertEquals(ohms("1k"), ohms("1K"), 1e-9)
    @Test fun `phẩy thập phân 4_7k`() = assertEquals(4700.0, ohms("4,7k"), 1e-9)
    @Test fun `chấm thập phân 4_7k`() = assertEquals(4700.0, ohms("4.7k"), 1e-9)
    @Test fun `kiểu giữa 4k7`() = assertEquals(4700.0, ohms("4k7"), 1e-9)
    @Test fun `kiểu giữa 1k5`() = assertEquals(1500.0, ohms("1k5"), 1e-9)
    @Test fun `mega 2M2`() = assertEquals(2_200_000.0, ohms("2M2"), 1e-9)
    @Test fun `giga 1g`() = assertEquals(1e9, ohms("1g"), 1e-9)
    @Test fun `có ký tự omega`() = assertEquals(4700.0, ohms("4,7kΩ"), 1e-9)
    @Test fun `có chữ ohm`() = assertEquals(1000.0, ohms("1k ohm"), 1e-9)
    @Test fun `có khoảng trắng đầu cuối`() = assertEquals(4700.0, ohms("  4,7k "), 1e-9)
    @Test fun `dưới 1 ôm 0,47`() = assertEquals(0.47, ohms("0,47"), 1e-9)
    @Test fun `rỗng là EMPTY`() = assertEquals(ValueErrorKind.EMPTY, err("   "))
    @Test fun `ký tự lạ là INVALID_FORMAT`() = assertEquals(ValueErrorKind.INVALID_FORMAT, err("4a7"))
    @Test fun `hai dấu phẩy là INVALID_FORMAT`() = assertEquals(ValueErrorKind.INVALID_FORMAT, err("4,7,0"))
    @Test fun `số 0 là NON_POSITIVE`() = assertEquals(ValueErrorKind.NON_POSITIVE, err("0"))
    @Test fun(`0k là NON_POSITIVE`() = assertEquals(ValueErrorKind.NON_POSITIVE, err("0k"))
    @Test fun `4R7 là SMD_STYLE`() = assertEquals(ValueErrorKind.SMD_STYLE, err("4R7"))
    @Test fun `r thường vẫn nhận ra SMD`() = assertEquals(ValueErrorKind.SMD_STYLE, err("4r7"))
}
```
(Lưu ý fix cú pháp dòng `@Test fun(` — phải là `@Test fun` thường.)

- [ ] **Step 2: Commit RED + push + poll CI → PHẢI failure** — commit `test(core): value parser tests (RED)`.

- [ ] **Step 3: Implement** — `BandRole.kt`:

```kotlin
package com.lkres.app.core

enum class BandRole { DIGIT, MULTIPLIER, TOLERANCE, TCR }

fun rolesFor(bandCount: Int): List<BandRole> = when (bandCount) {
    3 -> listOf(BandRole.DIGIT, BandRole.DIGIT, BandRole.MULTIPLIER)
    4 -> listOf(BandRole.DIGIT, BandRole.DIGIT, BandRole.MULTIPLIER, BandRole.TOLERANCE)
    5 -> listOf(BandRole.DIGIT, BandRole.DIGIT, BandRole.DIGIT, BandRole.MULTIPLIER, BandRole.TOLERANCE)
    6 -> listOf(BandRole.DIGIT, BandRole.DIGIT, BandRole.DIGIT, BandRole.MULTIPLIER, BandRole.TOLERANCE, BandRole.TCR)
    else -> emptyList()
}
```

`ValueParser.kt`:
```kotlin
package com.lkres.app.core

sealed interface ValueParseResult {
    data class Success(val ohms: Double) : ValueParseResult
    data class Error(val kind: ValueErrorKind) : ValueParseResult
}

enum class ValueErrorKind(val message: String) {
    EMPTY("Nhập giá trị cần tìm"),
    INVALID_FORMAT("VD hợp lệ: 4700 · 4,7k · 4.7k · 4k7 · 1M"),
    NON_POSITIVE("Giá trị phải lớn hơn 0"),
    SMD_STYLE("Mã dạng 4R7 là mã SMD — hãy tra ở tab SMD")
}

object ValueParser {

    private val SMD_R = Regex("""^\d*R\d+""")

    public fun parse(raw: String): ValueParseResult {
        val s = raw.trim().uppercase()
            .replace("Ω", "").replace("OHM", "").replace(" ", "")
        if (s.isEmpty()) return ValueParseResult.Error(ValueErrorKind.EMPTY)
        if ('R' in s && SMD_R.matches(s)) return ValueParseResult.Error(ValueErrorKind.SMD_STYLE)

        // Form 1: [digits][.,digits][K|M|G]   — 4700 / 4,7k / 1k
        val formA = Regex("""^(\d+)(?:[.,](\d+))?([KMG]?)$""").find(s)
        // Form 2: [digits][K|M|G][digits]     — 4k7 / 1k5
        val formB = Regex("""^(\d+)([KMG])(\d+)$""").find(s)

        val value: Double = when {
            formA != null -> {
                val (intPart, _, fracPart, mult) = formA.destructured
                base(intPart, fracPart) * multiplierOf(mult.firstOrNull())
            }
            formB != null -> {
                val (intPart, mult, fracPart) = formB.destructured
                base(intPart, fracPart) * multiplierOf(mult.first())
            }
            else -> return ValueParseResult.Error(ValueErrorKind.INVALID_FORMAT)
        }
        if (value <= 0.0) return ValueParseResult.Error(ValueErrorKind.NON_POSITIVE)
        return ValueParseResult.Success(value)
    }

    private fun base(intPart: String, fracPart: String): Double =
        (intPart + "." + fracPart.ifEmpty { "0" }).toDouble()

    private fun multiplierOf(c: Char?): Double = when (c) {
        'K' -> 1e3; 'M' -> 1e6; 'G' -> 1e9; else -> 1.0
    }
}
```
Ghi chú: destructured của formA có 3 group bắt (group 2 là dấu [.,] không bắt) — implementer kiểm chứng `destructured` khớp số group thật của Regex khi code (nếu sai số group thì dùng `groupValues` thay thế, ghi vào report).

- [ ] **Step 4: Commit GREEN + push + poll CI → PHẢI success** — commit `feat(core): band roles + value parser (TDD)`.

---

### Task 12: Core — ValueToColors (TDD)

**Files:**
- Create: `app/src/main/java/com/lkres/app/core/ValueToColors.kt`
- Test: `app/src/test/java/com/lkres/app/core/ValueToColorsTest.kt`

**Interfaces:**
- Consumes: `BandColor`, `ColorCode.MULTIPLIERS`
- Produces:
  - `data class ColorVariant(val bandCount: Int, val colors: List<BandColor>)` — colors CHỈ gồm digit(s) + multiplier (dung sai/TCR do UI gắn chip)
  - `sealed interface EncodingResult { data class Encodable(val variants: List<ColorVariant>); data class NotEncodable(val nearestE24: Double) }`
  - `object ValueToColors { fun encode(ohms: Double): EncodingResult }`

- [ ] **Step 1: Test FAIL trước** — `ValueToColorsTest.kt`:

```kotlin
package com.lkres.app.core

import org.junit.Assert.assertEquals
import org.junit.Assert.assertTrue
import org.junit.Test

class ValueToColorsTest {

    @Test
    fun `4700 ohm ra 4 dải vàng-tím-đỏ`() {
        val r = ValueToColors.encode(4700.0)
        assertTrue(r is EncodingResult.Encodable)
        r as EncodingResult.Encodable
        val v4 = r.variants.first { it.bandCount == 4 }
        assertEquals(listOf(BandColor.YELLOW, BandColor.VIOLET, BandColor.RED), v4.colors)
    }

    @Test
    fun `4700 ohm có thêm biến thể 5 dải vàng-tím-đen-nâu`() {
        val r = ValueToColors.encode(4700.0) as EncodingResult.Encodable
        val v5 = r.variants.first { it.bandCount == 5 }
        assertEquals(listOf(BandColor.YELLOW, BandColor.VIOLET, BandColor.BLACK, BandColor.BROWN), v5.colors)
    }

    @Test
    fun `4_7 ohm dùng multiplier vàng kim`() {
        val r = ValueToColors.encode(4.7) as EncodingResult.Encodable
        val v4 = r.variants.first { it.bandCount == 4 }
        assertEquals(listOf(BandColor.YELLOW, BandColor.VIOLET, BandColor.GOLD), v4.colors)
    }

    @Test
    fun `0_47 ohm dùng multiplier bạc`() {
        val r = ValueToColors.encode(0.47) as EncodingResult.Encodable
        val v4 = r.variants.first { it.bandCount == 4 }
        assertEquals(listOf(BandColor.YELLOW, BandColor.VIOLET, BandColor.SILVER), v4.colors)
    }

    @Test
    fun `47500 chỉ encode được 5 dải 3 digit`() {
        val r = ValueToColors.encode(47500.0) as EncodingResult.Encodable
        assertEquals(listOf(5, 6), r.variants.map { it.bandCount })
        val v5 = r.variants.first()
        assertEquals(listOf(BandColor.YELLOW, BandColor.VIOLET, BandColor.GREEN, BandColor.RED), v5.colors)
    }

    @Test
    fun `1234 không encode được gợi ý 1_2k gần nhất E24`() {
        val r = ValueToColors.encode(1234.0)
        assertTrue(r is EncodingResult.NotEncodable)
        assertEquals(1200.0, (r as EncodingResult.NotEncodable).nearestE24, 1e-9)
    }

    @Test
    fun `47k ra 4 dải vàng-tím-cam`() {
        val r = ValueToColors.encode(47000.0) as EncodingResult.Encodable
        val v4 = r.variants.first { it.bandCount == 4 }
        assertEquals(listOf(BandColor.YELLOW, BandColor.VIOLET, BandColor.ORANGE), v4.colors)
    }
}
```

- [ ] **Step 2: Commit RED → CI failure** — `test(core): value-to-colors tests (RED)`.

- [ ] **Step 3: Implement** — `ValueToColors.kt`:

```kotlin
package com.lkres.app.core

data class ColorVariant(val bandCount: Int, val colors: List<BandColor>)

sealed interface EncodingResult {
    data class Encodable(val variants: List<ColorVariant>) : EncodingResult
    data class NotEncodable(val nearestE24: Double) : EncodingResult
}

object ValueToColors {

    private val E24 = listOf(10, 11, 12, 13, 15, 16, 18, 20, 22, 24, 27, 30,
        33, 36, 39, 43, 47, 51, 56, 62, 68, 75, 82, 91)

    private val EXP_TO_COLOR = mapOf(
        -2 to BandColor.SILVER, -1 to BandColor.GOLD, 0 to BandColor.BLACK,
        1 to BandColor.BROWN, 2 to BandColor.RED, 3 to BandColor.ORANGE,
        4 to BandColor.YELLOW, 5 to BandColor.GREEN, 6 to BandColor.BLUE,
        7 to BandColor.VIOLET, 8 to BandColor.GRAY, 9 to BandColor.WHITE
    )

    private fun digitColor(d: Int): BandColor =
        BandColor.entries.first { ColorCode.DIGITS[it] == d }

    fun encode(ohms: Double): EncodingResult {
        require(ohms > 0.0) { "ohms phải > 0" }
        val variants = mutableListOf<ColorVariant>()

        val v4 = encodeWithDigits(ohms, 2)
        if (v4 != null) variants += ColorVariant(4, v4)
        val v5 = encodeWithDigits(ohms, 3)
        if (v5 != null) {
            variants += ColorVariant(5, v5)
            variants += ColorVariant(6, v5)
        }
        if (variants.isNotEmpty()) return EncodingResult.Encodable(variants)
        return EncodingResult.NotEncodable(nearestE24(ohms))
    }

    // significand = ohms / 10^exp ; trả colors nếu significand nguyên có đúng `digits` chữ số và exp hợp lệ
    private fun encodeWithDigits(ohms: Double, digits: Int): List<BandColor>? {
        val exp = ohms.log10().toInt() - (digits - 1)
        val multColor = EXP_TO_COLOR[exp] ?: return null
        val significand = ohms / Math.pow(10.0, exp.toDouble())
        val rounded = Math.round(significand * 1e9) / 1e9
        if (Math.abs(rounded - Math.rint(rounded)) > 1e-9) return null
        val s = rounded.toLong()
        if (s < Math.pow(10.0, (digits - 1).toDouble()) || s >= Math.pow(10.0, digits.toDouble())) return null
        val ds = s.toString().map { it - '0' }
        return ds.map { digitColor(it) } + multColor
    }

    private fun nearestE24(ohms: Double): Double {
        val e = Math.floor(ohms.log10()).toInt()
        var best = Double.MAX_VALUE
        var bestV = ohms
        for (de in e - 1..e + 1) {
            E24.forEach { base ->
                val v = base * Math.pow(10.0, (de - 1).toDouble())
                if (Math.abs(v - ohms) < best) { best = Math.abs(v - ohms); bestV = v }
            }
        }
        return bestV
    }

    private fun Double.log10(): Double = Math.log10(this)
}
```
Ghi chú implementer: kiểm chứng edge 100×10^n (significand "100" ở 2 digit → phải rơi nhánh 3 digit), giá trị < 1Ω (exp âm — 0.47 = 47×10⁻² → SILVER ✓). Nếu công thức exp lệch ở biên (VD 1000 = 100×10¹ 3-digit hay 10×10² 2-digit — cả hai đều hợp lệ, chấp nhận có cả 2 variant) → ghi kết quả thật vào report, test trên là chuẩn đối chiếu.

- [ ] **Step 4: Commit GREEN → CI success** — `feat(core): value to colors encoder (TDD)`.

---

### Task 13: Core — BandsState v2 (Auto/Manual + activeBand + undo) (TDD)

**Files:**
- Modify: `app/src/main/java/com/lkres/app/ui/bands/BandsState.kt` (viết lại)
- Test: `app/src/test/java/com/lkres/app/ui/bands/BandsStateTest.kt` (viết lại)

**Interfaces (Task 14/16 dùng):**
```kotlin
enum class BandsMode { AUTO, MANUAL }
class BandsState {
    val mode: BandsMode            // mặc định AUTO (đổi ở tab Cài đặt — Task 16)
    val bandCount: Int             // AUTO mặc định 3; MANUAL do setBandCount
    val selected: List<BandColor?> // độ dài = bandCount
    val activeBand: Int            // dải đang sửa (0-based)
    val canAddBand: Boolean        // bandCount < 6
    val canRemoveBand: Boolean     // bandCount > 3
    val result: CalcResult?        // null nếu còn dải trống
    fun setMode(m: BandsMode)      // giữ màu tối đa có thể
    fun setBandCount(n: Int)       // MANUAL: 3..6, giữ màu cũ
    fun addBand()                  // AUTO: +1 (max 6), khôi phục màu đã nhớ cho slot mới nếu có
    fun removeBand()               // AUTO: −1 (min 3), NHỚ màu dải bị bỏ (phục hồi khi add lại)
    fun setActiveBand(i: Int)
    fun moveActive(delta: Int)     // ◀/▶: activeBand + delta, kẹp trong 0..bandCount-1
    fun pick(color: BandColor)     // gán cho activeBand; hợp lệ theo vai (dùng rolesFor + bảng ColorCode);
                                   // sau pick KHÔNG tự nhảy dải (user tự ◀▶ hoặc chạm dải)
    fun applyColors(colors: List<BandColor?>) // điền CẢ dãy màu (dùng bởi Search — Task 15):
                                   // bandCount = colors.size, selected = colors, activeBand = 0
}
```
KHÔNG có undo()/clearAll() — user đã loại bỏ 2 nút này (sửa = chạm dải + chọn lại; search điền đè bằng applyColors).
Quy tắc vai khi pick: `BandRole.DIGIT` → màu phải có trong `ColorCode.DIGITS`; `MULTIPLIER` → `MULTIPLIERS`; `TOLERANCE` → `TOLERANCES` (bảng đầy đủ, UI sẽ tự lọc hiển thị 8 màu phổ biến — core vẫn nhận đủ); `TCR` → `TCR`. Pick không hợp lệ = no-op (UI không cho bấm nên không xảy ra; core tự vệ). Test: KHÔNG có test undo/clearAll (đã bỏ); có test moveActive kẹp biên, applyColors (đổi bandCount + điền + activeBand=0).

- [ ] **Step 1: Viết test FAIL** — bộ test mới thay file cũ (~14 test): auto mặc định 3 dải; addBand 3→4 giữ màu; removeBand 4→3 nhớ màu + addBand lại khôi phục; biên 6 không add được (canAddBand=false), 3 không remove được; pick gán đúng activeBand; pick sai vai no-op; moveActive kẹp biên (0 đi trái vẫn 0, cuối đi phải vẫn cuối); applyColors("4 dải") đổi bandCount + selected + activeBand=0; setMode AUTO↔MANUAL giữ màu; setBandCount manual giữ màu; rolesFor đúng 4 case. Tên test tiếng Việt rõ hành vi.
- [ ] **Step 2: RED → CI failure** — `test(ui): bands state v2 tests (RED)`.
- [ ] **Step 3: Implement** theo contract (pattern `_backingField` + getter để tránh JVM clash — bài học Task 7 v1.0; removeBand nhớ màu vào `rememberedColors: MutableMap<Int, BandColor>`).
- [ ] **Step 4: GREEN → CI success** — `feat(ui): bands state v2 auto/manual (TDD)`.
- [ ] **Step 5: Xoá `optionsFor` cũ trong BandsScreen.kt KHÔNG làm ở task này** — Task 14 thay toàn bộ UI, tránh xung đột.

---

### Task 14: UI — Tab Dải màu v2 (layout lai A+)

**Files:**
- Modify: `app/src/main/java/com/lkres/app/ui/bands/BandsScreen.kt` (viết lại toàn bộ)
- Modify: `app/src/main/java/com/lkres/app/ui/resistor/ResistorCanvas.kt` (thêm param viền dải active)

**Layout (baseline code implementer viết theo spec này, tra API Material 3 trước):**

KHU VỰC HÌNH — CỐ ĐỊNH, KHÔNG CUỘN (đặt NGOÀI vùng scroll; mọi nội dung khác nằm trong scrollable bên dưới):
```
┌──────────────────────────────────┐
│   ╭──── hình trở ────╮           │
│ ●─[vàng][tím][ĐỎ*][  ]─●         │  ← dải active có VIỀN SÁNG (viền primary
│   ╰──────────────────╯           │     hoặc trắng đậm + độ dày 3dp) vẽ trong Canvas
│        4.7 kΩ ±5%                │  ← Text giá trị NGAY DƯỚI trở, trong cùng khối hình
│  (◀) (1)(2)([3])(4) (▶)          │  ← mũi tên trái/phải (moveActive ∓1) + ô chỉ mục
└──────────────────────────────────┘     dải chạm được; ◀ mờ ở dải 1, ▶ mờ ở dải cuối
```
VÙNG CUỘN (dưới khối hình):
```
[Hàng chip theo LOẠI của activeBand — tiêu đề hàng: "Digit" / "Nhân" / "Dung sai" / "Hệ số nhiệt"]
   ← chip 40dp, chữ giá trị bên trong: DIGIT→"0".."9"; MULTIPLIER→"×1","×10",...,"×0.1","×0.01";
      TOLERANCE→"±1%","±2%","±0.5%","±0.25%","±0.1%","±0.05%","±5%","±10%" (8 màu phổ biến: nâu,đỏ,lục,lam,tím,xám,vàng kim,bạc);
      TCR→"100","50","15","25","20","10","5","1"
   ← chữ trắng/đen tự chọn theo độ sáng nền chip (Color.luminance())
   ← chip đã chọn của dải active viền primary đậm
[Cảnh báo rareWarning / lỗi Invalid — text nhỏ màu error]
```
- KHÔNG có nút Undo, KHÔNG có nút Xoá tất cả, KHÔNG có toggle Auto/Manual trên tab này (chuyển hết sang tab Cài đặt — Task 16).
- Chế độ hiện hành theo `state.mode`: AUTO → hàng nút `[-] [+]` (disable theo canAddBand/canRemoveBand) nằm ngay trên hàng chip; MANUAL → segmented 3-6 (giữ tương tự v1.0, tái dùng `rolesFor`).
- Chạm dải trên hình HOẶC ô chỉ mục HOẶC mũi tên → `setActiveBand/moveActive`.
- **ResistorCanvas thêm param `activeBandIndex: Int = -1`** — vẽ viền sáng quanh dải đó (stroke 3dp màu trắng/primary, vẽ SAU dải màu, trước bóng đáy; -1 = không viền). Giữ nguyên mọi param + lớp vẽ cũ.
- [ ] **Step 1: Tra cứu API** (FlowRow, Color.luminance, Canvas drawStroke/drawRoundRect stroke) — xác nhận qua docs.
- [ ] **Step 2: Implement** — commit `feat(ui): bands screen v2 (auto/manual, typed chip rows, band tap-edit)`.
- [ ] **Step 3: Push + CI success** (run id vào report).

---

### Task 15: UI — Search giá trị → tổ hợp màu song song

**Files:**
- Modify: `app/src/main/java/com/lkres/app/ui/bands/BandsScreen.kt` (thêm section Search trên cùng) — hoặc tách `SearchSection.kt` trong cùng package nếu file > 300 dòng (khuyến khích tách)

**Layout (nằm trong VÙNG CUỘN, dưới khối hình cố định của Task 14):**
```
[Ô search: "Nhập giá trị: 4700 · 4,7k · 4k7"]  [nút tìm]
— nếu lỗi parse: message theo ValueErrorKind.message (SMD_STYLE → gợi ý sang tab SMD)
— nếu NotEncodable: "Không encode được — gợi ý gần nhất: 1.3 kΩ" + nút "Dùng gợi ý"
— nếu Encodable: các THẺ song song (FlowRow hoặc Row cuộn ngang):
   ┌ 4 dải: [vàng][tím][đỏ]  Dung sai: (±1%)(±2%)(±5%)(±10%) ┐
   ┌ 5 dải: [vàng][tím][đen][nâu]  Dung sai: (...)           ┐
   ┌ 6 dải: như 5 dải + TCR: (100)(50)(15)(25)(10)(5)(1)(trống) ┐
— bấm thẻ/chip nào → state màu của BandsState được ĐIỀN theo tổ hợp đó (bandCount đổi theo thẻ,
  dung sai/TCR điền vào dải tương ứng) → hình trở vẽ lại → user đối chiếu với trở thật
— Setting `parallelResults` (Task 16) = false → chỉ hiện thẻ 4 dải (nếu không có 4 dải thì thẻ đầu tiên)
```
- Chip dung sai/TCR trong thẻ: chọn 1 trong các mức (radio-style), mặc định ±5%; TCR mặc định TRỐNG (dải 6 chưa có màu).
- [ ] **Step 1: Tra cứu API** (FlowRow, FilterChip/AssistChip).
- [ ] **Step 2: Implement** — commit `feat(ui): value search with parallel color variants`.
- [ ] **Step 3: Push + CI success.**
- Lịch sử search hiển thị dưới ô search (chips gợi ý lại) — LÀM Ở TASK 16 sau khi có DataStore; task này để chỗ trống (comment `// TODO history Task 16` trong code là KHÔNG được phép — chỉ bỏ qua phần history, Task 16 sẽ thêm).

---

### Task 16: Tab Cài đặt + DataStore persistence + lịch sử

**Files:**
- Create: `app/src/main/java/com/lkres/app/data/LkResStore.kt` (DataStore — lớp Android DUY NHẤT)
- Create: `app/src/main/java/com/lkres/app/ui/settings/SettingsScreen.kt`
- Modify: `LKResApp.kt` (thêm tab thứ 4 "Cài đặt", route "settings", icon gear tự vẽ ImageVector như 3 icon cũ)
- Modify: `BandsScreen.kt` (nạp state từ store lúc khởi động; lưu mỗi thay đổi; history chips dưới search)
- Modify: `ui/settings/SettingsScreen.kt` — chứa thêm toggle **Chế độ chọn màu: Auto / Manual** (SegmentedButton 2 ô, ghi vào `mode` của store; BandsScreen đọc theo state)

**Yêu cầu:**
- DataStore Preferences (không dùng SharedPreferences cũ, không Room — YAGNI). Keys: `mode` ("AUTO"/"MANUAL", default AUTO), `band_count`, `selected` (tên enum cách nhau bằng dấu phẩy, ô trống = `-`), `active_band`, `parallel_results` (default true), `recent_searches` (JSON array string, tối đa N mục).
- LƯU mỗi khi state đổi (gọi save ở mutation points, KHÔNG dùng listener phức tạp). Nạp MỘT LẦI lúc mở app (trước khi user tương tác; trong lúc chờ đọc thì hiển thị state mặc định — chấp nhận flash nhẹ).
- Lịch sử: mỗi lần search THÀNH CÔNG (Encodable) → thêm đầu danh sách (không trùng lặp liên tiếp), giữ tối đa N. **N = subagent tự search web best-practice** ("recent searches UX how many items best practice") — dự kiến 5, ghi nguồn vào report. Hiển thị: chips dưới ô search ("4,7k" "1k" ...), bấm → điền + tra lại; nút xoá lịch sử.
- SettingsScreen: **Chế độ chọn màu: Auto / Manual**; toggle "Hiển thị song song nhiều tổ hợp màu khi tìm kiếm" (mặc định BẬT); toggle "Lịch sử tra cứu" (bật/tắt ghi lịch sử, mặc định BẬT) + nút "Xoá lịch sử"; ghi chú phiên bản app.
- [ ] **Step 1: Tra cứu DataStore Preferences API hiện tại + best-practice số mục history** (DuckDuckGo/webfetch, ghi nguồn).
- [ ] **Step 2: Implement** — commit `feat: settings tab + datastore persistence + recent searches`.
- [ ] **Step 3: Push + CI success.**
- LƯU Ý clash: BandsState đã có getter `bandCount` — khi thêm field mode/activeBand dùng pattern `_backing` nhất quán.

---

### Task 17: UI — Tham khảo bảng cột cố định

**Files:**
- Modify: `app/src/main/java/com/lkres/app/ui/reference/ReferenceScreen.kt` (viết lại)

**Yêu cầu:**
- Mỗi bảng = lưới cột TRỌNG SỐ CỐ ĐỊNH (Modifier.weight) — hàng tiêu đề + các hàng dữ liệu cùng weight → cột thẳng đứng không xê dịch:
  - Bảng màu chuẩn: `Màu(sw 40dp) | Tên(w 1.2) | Digit(w 0.8) | Nhân(w 1.3) | Dung sai(w 1.1) | TCR(w 1.1)`
  - Bảng dung sai: `Màu | Tên | Giá trị` (kèm hàng "Không dải — ±20%" như v1.0)
  - Bảng TCR: `Màu | Tên | ppm/°C`
  - EIA-96: legend chữ cái grid cố định + bảng 96 mã (nội dung giữ từ v1.0)
- Text trong ô không wrap làm lệch cột: `maxLines = 1` + font nhỏ dần nếu dài (AutoShrink không có sẵn → dùng fontSize nhỏ hơn cho cột Nhân, hoặc `softWrap = false` + `overflow = TextOverflow.Ellipsis` — chọn và ghi vào report)
- Vẫn render số từ `core/` — cấm hard-code
- [ ] **Step 1: Tra cứu** Row weight trong LazyColumn (Row scope weight OK trong item).
- [ ] **Step 2: Implement** — commit `feat(ui): fixed-column reference tables`.
- [ ] **Step 3: Push + CI success.**

---

### Task 18: EIA-96 — tối ưu hiệu năng (research trước)

**Files:**
- Modify: `app/src/main/java/com/lkres/app/ui/reference/ReferenceScreen.kt` (phần EIA-96)

**BẮT BUỘC research trước khi code (Luật 3):** DuckDuckGo search "jetpack compose lazy column performance best practices keys contentType" + "compose lazy grid vs column many items" — gom ≥3 kỹ thuật áp dụng được, ghi nguồn + kỹ thuật chọn vào report. Kỹ thuật kỳ vọng: `remember` danh sách đã sort/chunk (không tính lại mỗi recomposition), `key` cố định từng item, `contentType`, chuyển 96 hàng đơn → grid 3 cột (32 hàng) bằng `chunked(3)` trong LazyColumn (tránh LazyVerticalGrid lồng nhau), cân nhắc `derivedStateOf` cho filter.
- Nếu sau tối ưu vẫn có nguy cơ lag máy yếu → bọc EIA-96 trong section thu gọn (Expanded mặc định TẮT, nút mở) — quyết định dựa trên research, ghi lý do.
- [ ] **Step 1: Research + ghi nguồn.** **Step 2: Implement** — commit `perf(ui): eia-96 grid + lazy optimizations`. **Step 3: Push + CI success.**

---

### Task 19: Verify tổng trên điện thoại user

- [ ] Push cuối → CI xanh (full unit test + APK artifact).
- [ ] User cài APK, checklist: Auto mặc định 3 dải +/− giữ màu; chip hiện số đúng loại; chạm dải sửa được; Undo/Xoá; Manual như cũ; search "4700"/"4,7k"/"4k7"/"1K" ra thẻ song song, đổi chip dung sai đổi dải cuối; "4R7" báo sang SMD; "1234" gợi ý 1.3k; thoát app vào lại giữ nguyên màu + cài đặt; lịch sử hiện + xoá được; Tham khảo cột thẳng; EIA-96 không lag.
- [ ] User xác nhận → đóng v1.1.

## Self-Review (đã chạy khi viết)

1. **Coverage feedback user:** auto+/− giữ màu (T13), chip số theo loại (T14), Undo/Xoá tất cả (T13/14), search đa định dạng + song song + chip sai số/TCR (T11/12/15), thắc mắc sai số/TCR = chip chọn không tự tính (T15), tab cài đặt (T16), lưu state + lịch sử (T16), bảng cố định (T17), EIA-96 lag + research (T18). Không gap.
2. **Placeholder scan:** không TBD; các chỗ "implementer viết theo contract" đều có contract cụ thể (T13) hoặc baseline layout chi tiết (T14/15).
3. **Type consistency:** `BandRole`/`rolesFor` dùng chung T13/14; `ValueParseResult`/`EncodingResult` khớp T11/12/15; `BandsState` API mới khớp T14/16; tránh JVM clash bằng `_backing`.

---

### Task 20: Viền active + live search + fix bug song song OFF (feedback user sau v1.1)

**Files:**
- Modify: pp/src/main/java/com/lkres/app/ui/resistor/ResistorCanvas.kt
- Modify: pp/src/main/java/com/lkres/app/ui/bands/SearchSection.kt (+ BandsScreen.kt nếu wiring cần)

**Yêu cầu 1 — Viền dải active (ResistorCanvas):**
- Bỏ stroke chữ nhật nhô ra ngoài. Viền mới: vẽ TRONG clipPath(body) cùng lúc với dải màu (thêm stroke quanh rect dải active BÊN TRONG clip) → viền ôm theo dáng thân, không nhô ra ngoài góc
- Độ dày ~2dp; bo góc nhẹ (CornerRadius ~3dp trên rect viền)
- Màu viền = **màu dải hiện tại pha sáng** (lerp về trắng ~45%); dải TRỐNG (null) → trắng mờ (alpha ~0.7). Giữ param ctiveBandIndex: Int = -1

**Yêu cầu 2 — Fix bug song song OFF (SearchSection):**
- Đọc code thật tìm root cause: khi parallelResults=false chỉ hiện 1 thẻ mà bấm thẻ/chip không applyColors. Sửa để hành vi ĐỒNG NHẤT giữa ON/OFF (tap thẻ/chip đều apply). Ghi root cause vào report

**Yêu cầu 3 — Live search (SearchSection):**
- BỎ nút "Tìm". Mỗi lần text đổi: parse → encode thành công → TỰ pplyColors tổ hợp MẶC ĐỊNH (variant 4 dải nếu có, không thì variant đầu; dung sai mặc định ±5% GOLD; TCR trống) — user chưa bấm gì vẫn thấy màu chạy theo từng ký tự
- Parse lỗi/rỗng → KHÔNG đụng state hình trở (giữ nguyên), chỉ hiện hint lỗi nhẹ
- Các thẻ 4/5/6 dải vẫn hiện như cũ (gate parallelResults); bấm thẻ/chip khác → đè tổ hợp đó
- History: KHÔNG ghi mỗi keystroke — chỉ ghi khi user BẤM thẻ/chip (explicit choice)
- Chú ý performance: parse+encode rẻ; vẫn nên emember theo text để tránh tính lại thừa

**QUY TRÌNH:** đọc file thật trước (mô tả có thể lệch — khác biệt thì DỪNG báo cáo) → implement → commit eat(ui): live search + active band border v2 + fix single-card apply → push → poll CI (GET https://api.github.com/repos/auhsuai/LKRes/actions/runs?per_page=1, header User-Agent bất kỳ, 30s/lần, tối đa 15 phút) → PHẢI success. Encoding UTF-8 sạch.

**Report:** .superpowers/sdd/task-20-report.md. Trả về CHỈ: status, commit hash, run id + conclusion, root cause bug song song OFF, lệch so với brief (nếu có), concerns.

---

### Task 21: 6 chỉnh nhỏ theo feedback user (sau Task 20)

**Files:** ResistorCanvas.kt, SearchSection.kt, SettingsScreen.kt, LkResStore.kt, LKResApp.kt (+ MainActivity.kt nếu cần cho keep-screen-on)

1. **Tiêu đề hàng "Digit" → "Giá trị"** (BandsScreen.kt — tiêu đề loại dải; các tiêu đề khác giữ: Nhân/Dung sai/Hệ số nhiệt)
2. **Nhãn chế độ Cài đặt → "Tự động" / "Thủ công"** (SettingsScreen.kt — nếu đang tiếng Anh thì đổi; đọc code thật)
3. **Setting mới "Giữ màn hình sáng khi dùng app"** (mặc định TẮT): key DataStore keep_screen_on; áp Window.FLAG_KEEP_SCREEN_ON theo setting (SideEffect/DisposableEffect + LocalContext as Activity). BẮT BUỘC research best-practice chính thức (developer.android.com — prefer FLAG_KEEP_SCREEN_ON hơn wake lock) ghi nguồn report
4. **Viền active nổi hơn:** tự tương phản theo luminance — dải tối (lum<0.5) → viền lerp trắng 60%; dải sáng → viền lerp ĐEN 55%; dày 2.5dp; giữ clip + bo góc; dải trống → trắng mờ như cũ
5. **Đổi tên tab:** "Dải màu" → "Trở cắm"; "SMD" → "Trở dán" (LKResApp.kt — label + contentDescription)
6. **Bỏ chip Dung sai/TCR khỏi thẻ search:** thẻ chỉ còn tiêu đề + dải màu digit+nhân (gọn). Bấm thẻ → applyColors = colors + tolerance GOLD ±5% mặc định (để giá trị hiện ngay, user chỉnh sau bằng chạm dải dung sai trên hình) + TCR null nếu 6 dải (giữ hành vi spec cũ). Chọn dung sai/TCR sau = chạm dải trên hình (cơ chế active band có sẵn). History vẫn ghi khi bấm thẻ

**QUY TRÌNH:** đọc file thật trước (khác biệt → DỪNG báo) → research keep-screen-on → implement (checkpoint: store → settings → canvas → search/tab) → commit eat(ui): task 21 polish (labels, keep screen on, contrast border, slim cards) → push → poll CI (quy trình cũ) → PHẢI success. Encoding sạch.

**Report:** .superpowers/sdd/task-21-report.md. Trả về CHỈ: status, commit hash, run id + conclusion, nguồn research keep-screen-on, root cause nếu có lệch, concerns.

---

### Task 22: Chuyên nghiệp hoá toàn bộ text + viền nổi rõ (feedback user)

**Files:** ResistorCanvas.kt, BandsScreen.kt, SearchSection.kt, SettingsScreen.kt, ReferenceScreen.kt, SmdScreen.kt, LKResApp.kt (nếu có string), core/{ResistorCalculator,ValueParser,SmdParser,ResistorFormat}.kt + test ResistorFormatTest (decimal comma)

**A. Viền active nổi rõ (ResistorCanvas):** bỏ lerp 55-60% (bị chìm trên dải sáng). Mới: dải tối (lum<0.5) → viền TRẮNG tinh Color.White; dải sáng → viền GẦN ĐEN Color(0xFF141414); dày 3dp; giữ clip + bo + dải trống trắng mờ. Nguyên tắc selection indicator: tương phản cực đại.

**B. Rà + viết lại TOÀN BỘ string user-facing (research bắt buộc):**
- Research: (1) Material Design content design guidelines (m3.material.io/foundations/content-design — ngắn gọn, không jargon, actionable); (2) DuckDuckGo thuật ngữ điện tử Việt chuẩn: "chữ số", "hệ số nhân", "dung sai", "hệ số nhiệt độ". Ghi nguồn report.
- Nguyên tắc viết lại: KHÔNG dùng gạch ngang dài (—); KHÔNG trộn từ Anh không cần thiết (Digit→chữ số, encode→"không có giá trị chuẩn"); câu ngắn, trung tính; ví dụ đặt sau dấu hai chấm, phân cách bằng dấu phẩy.
- Sweep TOÀN BỘ file nêu trên (grep Text( + các message trong core): VD đổi:
  - "Digit đầu là trắng (9) — hiếm gặp trên điện trở thật" → "Chữ số đầu là 9 (trắng), hiếm gặp trong thực tế"
  - "Không encode được — gợi ý gần nhất: X" → "Giá trị không nằm trên dải chuẩn. Giá trị chuẩn gần nhất: X"
  - "Mã dạng 4R7 là mã SMD — hãy tra ở tab SMD" → "Đây là mã trở dán. Hãy tra ở tab Trở dán"
  - "VD hợp lệ: 4700 · 4,7k · 4.7k · 4k7 · 1M" → "Ví dụ: 4700, 4,7k, 4k7, 1M"
  - "Dùng gợi ý" → "Dùng giá trị này"; label search → "Nhập giá trị cần tìm (4700, 4,7k, 4k7)"
  - SmdScreen hint + các string khác tự rà theo nguyên tắc (liệt kê bảng cũ→mới trong report)
- **Decimal comma Việt:** ResistorFormat hiển thị "4,7 kΩ" (dấu phẩy) thay "4.7". TDD: sửa test TRƯỚC (4.7→4,7 toàn bộ ResistorFormatTest + case TCR) → RED → đổi ormatNumber → GREEN. Kiểm ValueParser vẫn parse được chuỗi history "4,7 kΩ" (đã hỗ trợ phẩy ✓)

**QUY TRÌNH:** research → đọc toàn bộ file thật, lập BẢNG string cũ→mới trong report → TDD phần format → implement → commit efactor(ui): professional vietnamese copy + high contrast active border → push → poll CI (quy trình cũ) → PHẢI success. Encoding sạch.

**Report:** .superpowers/sdd/task-21-report.md đổi tên... KHÔNG — dùng .superpowers/sdd/task-22-report.md. Trả về CHỈ: status, commit hash, run id + conclusion, nguồn research, số string đã đổi (bảng tóm tắt), concerns.

---

### Task 24: Tái cấu trúc layout tab Trở cắm + bỏ lịch sử (feedback user)

**Files:** BandsScreen.kt, SearchSection.kt, SettingsScreen.kt, LkResStore.kt

**1. Bố cục mới tab Trở cắm (BandsScreen):**
- Cố định trên: SearchSection (chỉ ô tìm kiếm — không history)
- Cố định giữa: khối hình trở + giá trị
- Vùng cuộn (weight 1f): segmented 3-6 (chỉ MANUAL) + hàng chip theo loại + text lỗi
- Cố định ĐÁY (trên bottom nav): thanh chuyển dải tích hợp:
  - AUTO: [−] (◀) (1)(2)(3)(4) (▶) [+] — −/+ 36dp disable theo canRemoveBand/canAddBand
  - MANUAL: (◀) (1)(2)(3)(4) (▶) — segmented 3-6 giữ ở vùng cuộn như cũ

**2. SearchSection:** XOÁ dòng Text giá trị trong nhánh Results (trùng với giá trị dưới hình trở — chỉ giữ 1 chỗ duy nhất dưới trở). XOÁ chip lịch sử + addRecentSearch + import LkResStore.

**3. XOÁ HẲN tính năng lịch sử:** LkResStore: xoá recentSearches/historyEnabled/addRecentSearch/RECENT_SEARCH_LIMIT + keys ecent_searches/history_enabled (persist + restore); SettingsScreen: xoá toggle Lịch sử + nút Xoá lịch sử. Grep ecentSearch|historyEnabled|Lịch sử = 0 sau khi xong.

**QUY TRÌNH:** đọc file thật (checkpoint: LkResStore → SettingsScreen → SearchSection → BandsScreen) → implement → commit efactor(ui): layout reorder, integrated band bar, remove history → push → poll CI (quy trình cũ) → PHẢI success. Encoding sạch. KHÔNG đụng core/, ResistorCanvas, test, workflow.

**Report:** .superpowers/sdd/task-24-report.md. Trả về CHỈ: status, commit hash, run id + conclusion, xác nhận 3 nhóm mục, concerns.
